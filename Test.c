#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>

int main() {
    int *ans = malloc(sizeof(int) * 3);
    for(int i = 0; i < )
    if(ans[1] == NULL) printf("HEYHEYEHYE");
}